const { log } = console;
// log("hello");

window.onload = () => {
	// test.innerText = main_section_1__id.offsetWidth;
	test.innerText = document.body.offsetWidth;
};
window.onresize = () => {
	// test.innerText = main_section_1__id.offsetWidth;
	test.innerText = document.body.offsetWidth;
};
